"""Case-study visualization placeholders for qualitative analysis."""

from __future__ import annotations


def case_study_template() -> dict[str, str]:
    """Return scaffold metadata for case-study reporting workflow."""
    return {
        "status": "TODO",
        "notes": "Add event-level timeline + map snapshots + model explanation panels.",
    }
