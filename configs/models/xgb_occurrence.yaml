name: "xgb_occurrence"
params:
  objective: "binary:logistic"
  eval_metric: "aucpr"
  n_estimators: 1000
  learning_rate: 0.03
  max_depth: 8
  subsample: 0.8
  colsample_bytree: 0.8
  reg_lambda: 1.0
  random_state: 42
  tree_method: "hist"
  early_stopping_rounds: 50
